## Packages
framer-motion | For the splash screen animation and page transitions
lucide-react | For medical and UI icons
clsx | For conditional class names
tailwind-merge | For merging tailwind classes safely

## Notes
RTL layout direction is required for Arabic support.
LocalStorage is used for persisting user sessions.
Editors are restricted to specific email addresses.
Backend APIs are defined in shared/routes.ts.
